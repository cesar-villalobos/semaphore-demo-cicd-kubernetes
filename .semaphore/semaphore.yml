version: v1.0
name: Semaphore CI/CD Kubernetes Demo
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Docker Build
    task:
      prologue:
        commands:
          - checkout
          - echo $DOCKERHUB_PASSWORD | docker login -u $DOCKERHUB_USERNAME --password-stdin
      jobs:
        - name: Build
          commands:
            - 'docker pull $DOCKERHUB_REGISTRY_URL:latest || true'
            - 'docker build --cache-from $DOCKERHUB_REGISTRY_URL:latest -t $DOCKERHUB_REGISTRY_URL:$SEMAPHORE_WORKFLOW_ID .'
            - 'docker push $DOCKERHUB_REGISTRY_URL:$SEMAPHORE_WORKFLOW_ID'
      secrets:
        - name: dockerhub
  - name: Tests
    task:
      env_vars:
        - name: NODE_ENV
          value: test
      prologue:
        commands:
          - docker login -u $DOCKERHUB_USERNAME -p $DOCKERHUB_PASSWORD
          - 'docker pull $DOCKERHUB_REGISTRY_URL:$SEMAPHORE_WORKFLOW_ID'
      jobs:
        - name: Static test
          commands:
            - 'docker run -it $DOCKERHUB_REGISTRY_URL:$SEMAPHORE_WORKFLOW_ID npm run lint'
        - name: Integration test
          commands:
            - sem-service start postgres
            - 'docker run --net=host -it $DOCKERHUB_REGISTRY_URL:$SEMAPHORE_WORKFLOW_ID npm run test'
        - name: Functional test
          commands:
            - sem-service start postgres
            - 'docker run --net=host -it $DOCKERHUB_REGISTRY_URL:$SEMAPHORE_WORKFLOW_ID npm run ping'
            - 'docker run --net=host -it $DOCKERHUB_REGISTRY_URL:$SEMAPHORE_WORKFLOW_ID npm run migrate'
      secrets:
        - name: dockerhub
  - name: Push as latest
    task:
      jobs:
        - name: Push
          commands:
            - docker login -u $DOCKERHUB_USERNAME -p $DOCKERHUB_PASSWORD
            - 'docker pull $DOCKERHUB_REGISTRY_URL:$SEMAPHORE_WORKFLOW_ID'
            - 'docker tag $DOCKERHUB_REGISTRY_URL:$SEMAPHORE_WORKFLOW_ID $DOCKERHUB_REGISTRY_URL:latest'
            - 'docker push $DOCKERHUB_REGISTRY_URL:latest'
      secrets:
        - name: dockerhub
