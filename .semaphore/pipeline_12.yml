version: v1.0
name: DigitalOcean
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Push
    task:
      jobs:
        - name: Image
          commands:
            - docker login -u $DO_USER -p $DO_PASSWORD $DO_REGISTRY
      secrets:
        - name: do-key
        - name: do_registry
        - name: dockerhub
